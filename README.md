# NEXXTY ONE

Versão GitHub Pages em um único `index.html`.

Coloque `index.html` na raiz do repositório e ative GitHub Pages em Settings → Pages → Deploy from branch → main → /(root).

Os leads ficam no navegador (localStorage). A busca consulta Nominatim e Overpass.
